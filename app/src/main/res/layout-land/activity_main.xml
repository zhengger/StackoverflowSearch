<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/et_SearchTopic"
        android:minWidth="100dp"
        android:padding="@dimen/padding_general"
        android:freezesText="true"
        android:hint="Search topic"
        android:layout_centerVertical="true"
        android:layout_toLeftOf="@+id/button_search"
        android:textSize="@dimen/text_size_large"
        android:layout_marginRight="@dimen/margin_general"
        android:layout_marginBottom="@dimen/margin_general"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Search"
        android:id="@+id/button_search"
        android:padding="@dimen/padding_general"
        style="@style/btnStyleOrange"
        android:layout_alignBottom="@+id/et_SearchTopic"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:textSize="@dimen/text_size_large"
        android:layout_marginRight="@dimen/margin_general" />

    <CheckBox
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Apply Android tag to search"
        android:id="@+id/AndroidTagCheckbox"
        android:layout_below="@+id/et_SearchTopic"
        android:layout_centerHorizontal="true"
        android:checked="false"
        android:textSize="@dimen/text_size_small"
        android:padding="@dimen/padding_android_tag" />
</RelativeLayout>
